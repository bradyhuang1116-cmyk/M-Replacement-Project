# 三菱图纸替换系统 - 客户端部署指南

## 部署前准备

### 检查清单
- [ ] 确认操作系统：Windows 10/11 或 Linux
- [ ] 确认有管理员权限
- [ ] 确认网络连接正常（需要下载依赖）

---

## 方案A：Docker部署（推荐，最简单）

### 1. 安装 Docker Desktop

**Windows系统**：
1. 下载 Docker Desktop：https://www.docker.com/products/docker-desktop
2. 双击安装包，按提示完成安装
3. 重启电脑
4. 打开 Docker Desktop，等待启动完成

**验证安装**：
```bash
docker --version
docker-compose --version
```

### 2. 获取项目代码

**方式1：从GitHub下载**
```bash
git clone https://github.com/bradyhuang1116-cmyk/Mitsubishi-Electric-Drawing-Replacement-Project.git
cd Mitsubishi-Electric-Drawing-Replacement-Project
```

**方式2：直接复制项目文件夹**
- 将整个项目文件夹复制到客户电脑
- 打开命令行，进入项目目录

### 3. 启动服务

```bash
# 构建并启动所有服务
docker-compose up -d

# 查看启动状态
docker-compose ps

# 查看日志（如果有问题）
docker-compose logs -f
```

### 4. 访问服务

- **Gradio Web界面**：http://localhost:7860
- **API文档**：http://localhost:8000/docs
- **健康检查**：http://localhost:8000/api/v1/health

### 5. 停止服务

```bash
docker-compose down
```

---

## 方案B：本地Python环境部署

### 1. 安装 Python 3.11 或 3.12

**Windows系统**：
1. 下载 Python：https://www.python.org/downloads/
2. 选择 Python 3.11.x 或 3.12.x 版本
3. 安装时勾选 "Add Python to PATH"
4. 完成安装

**验证安装**：
```bash
python --version
# 应显示：Python 3.11.x 或 3.12.x
```

### 2. 获取项目代码

同方案A的步骤2

### 3. 安装依赖

```bash
# 进入项目目录
cd Mitsubishi-Electric-Drawing-Replacement-Project

# 安装依赖（需要5-10分钟）
pip install -r requirements.txt
```

**如果安装失败**：
```bash
# 使用国内镜像源
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### 4. 环境检查

```bash
python env_checker.py
```

**预期输出**：
```
============================================================
环境检查报告
============================================================
  Python 3.11.x ✓
  paddleocr 2.7.3 ✓
  paddlepaddle 2.6.1 ✓
  fastapi 0.109.2 ✓
  gradio 4.19.2 ✓
  cv2 4.8.1 ✓
  PIL 10.2.0 ✓
  numpy 1.24.4 ✓
============================================================
✓ 环境检查通过
```

### 5. 启动服务

**Windows系统**：
```bash
# 启动Gradio界面
start.bat gradio

# 或启动API服务
start.bat api
```

**Linux/Mac系统**：
```bash
# 启动Gradio界面
./start.sh gradio

# 或启动API服务
./start.sh api
```

### 6. 访问服务

- **Gradio界面**：http://localhost:7860
- **API服务**：http://localhost:8000/docs

---

## 验证部署

### 测试1：访问Web界面
1. 打开浏览器访问：http://localhost:7860
2. 应该看到图纸上传界面
3. 上传一个测试图纸（TIF/PDF/JPG）
4. 点击"处理"按钮
5. 等待处理完成，查看结果

### 测试2：API接口测试
```bash
# 健康检查
curl http://localhost:8000/api/v1/health

# 处理图纸（需要准备测试文件）
curl -X POST "http://localhost:8000/api/v1/drawings/process" \
  -F "file=@test.tif"
```

---

## 常见问题

### Q1：Python版本不对怎么办？
**A**：必须使用Python 3.11或3.12。如果版本不对：
- 卸载当前Python
- 重新安装正确版本
- 或使用Docker部署（推荐）

### Q2：pip install 很慢或失败
**A**：使用国内镜像源：
```bash
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### Q3：PaddlePaddle安装失败
**A**：这是最常见的问题，建议：
1. 确认Python版本正确（3.11或3.12）
2. 使用Docker部署（推荐）
3. 或手动安装：
```bash
pip install paddlepaddle==2.6.1 -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### Q4：端口被占用
**A**：修改端口配置：
- 复制 .env.example 为 .env
- 修改 API_PORT=8000 为其他端口
- 重启服务

### Q5：Docker启动失败
**A**：
1. 确认Docker Desktop正在运行
2. 检查是否有权限问题
3. 查看日志：`docker-compose logs`

---

## 生产环境建议

### 1. 使用Docker部署
- 环境隔离，避免依赖冲突
- 一键启动，易于维护
- 跨平台兼容

### 2. 配置持久化
```bash
# 创建 .env 文件
cp .env.example .env

# 根据实际情况修改配置
```

### 3. 数据备份
- 定期备份 output/ 目录
- 保存处理日志

### 4. 性能优化
- 单次处理时间：约20-30秒/张
- 建议配置：4核CPU，8GB内存
- 大批量处理建议使用API接口

---

## 技术支持

如遇到问题，请提供以下信息：
1. 操作系统版本
2. Python版本（`python --version`）
3. 错误日志截图
4. env_checker.py 的输出结果
