# 图纸编号替换系统 — 项目需求与本地部署指南

## 一、项目目标与需求

### 1.1 项目概述

本项目为三菱电机图纸批量编号替换系统，用于自动识别工程图纸上的零件编号，并在编号前添加"H"前缀（如 `YA026D941` → `HYA026D941`）。

### 1.2 功能需求

| 功能 | 说明 |
|------|------|
| **矢量PDF替换** | 对矢量PDF文件，直接提取文本层进行精确替换，不依赖OCR |
| **OCR图像替换** | 对扫描版PDF、TIF、JPG等图像文件，通过OCR识别编号后进行像素级替换 |
| **多区域检测** | 自动检测三个关键区域：红框（材料编号列）、绿框（右下角编号）、橙框（左上角编号） |
| **四方投票校验** | 绿框、橙框、红框、文件名四方交叉校验，确保替换准确性 |
| **Web操作界面** | Gradio Web界面，支持文件上传、检测预览、确认替换的两步工作流 |
| **批量处理** | 支持同时上传多张图纸批量处理 |

### 1.3 替换规则

- 识别以指定首字母开头的编号（默认 X、Y），总长度 ≥ 7 字符
- 在编号前添加前缀 `H`
- 红框区域：逐单元格识别替换，支持删除线检测
- 绿框/橙框：四方投票确定编号后整体替换

### 1.4 输出规则

| 输入格式 | 处理模式 | 输出格式 | 输出文件夹 | 文件命名 |
|----------|----------|----------|------------|----------|
| 矢量PDF | 直接文本替换 | PDF | `output/latest/vector/` | `H` + 原文件名 + `.pdf` |
| 扫描PDF/TIF | OCR识别替换 | TIF | `output/latest/ocr/` | `H` + 原文件名 + `-R.tif` |
| JPG/PNG | OCR识别替换 | 原格式 | `output/latest/ocr/` | `H` + 原文件名 + `-R` + 原扩展名 |

---

## 二、本地部署指南

### 2.1 系统要求

| 项目 | 要求 |
|------|------|
| 操作系统 | Windows 10/11 或 Linux |
| Python | 3.11 或 3.12（**不支持 3.14**，PaddlePaddle 不兼容） |
| CPU | 4核以上 |
| 内存 | 8 GB 以上 |
| 磁盘 | 约 3 GB（含模型文件） |
| GPU（可选） | NVIDIA GPU + CUDA（可加速OCR，非必须） |

### 2.2 安装步骤

#### 第一步：克隆项目

```bash
git clone <项目仓库地址>
cd Mitsubishi-Electric-Drawing-Replacement-Project
```

#### 第二步：创建 Python 虚拟环境

**Windows:**
```bash
py -3.12 -m venv .venv
.venv\Scripts\activate
```

**Linux/Mac:**
```bash
python3.12 -m venv .venv
source .venv/bin/activate
```

#### 第三步：安装依赖

**CPU 模式（推荐）：**
```bash
pip install -r requirements_local.txt
```

依赖清单：
| 包名 | 版本要求 | 用途 |
|------|----------|------|
| paddlepaddle | ==3.0.0 | OCR推理引擎 |
| paddleocr | >=2.7 | OCR文字识别 |
| PyMuPDF | >=1.23 | PDF解析与渲染 |
| opencv-python-headless | >=4.8 | 图像处理 |
| Pillow | >=10.0 | 图像读写 |
| numpy | >=1.24 | 数值计算 |
| tqdm | >=4.65 | 进度条 |
| gradio | >=4.40.0 | Web界面 |

**GPU 模式（可选，需要 NVIDIA GPU + CUDA）：**
```bash
# 先安装 GPU 版 PaddlePaddle（替代 CPU 版）
pip install paddlepaddle-gpu==3.0.0
pip install -r requirements_local.txt
```

#### 第四步：验证安装

```bash
python -c "import paddleocr; import fitz; import gradio; print('依赖安装成功')"
```

### 2.3 启动服务

**方式一：直接启动**
```bash
python web_app.py
```
启动后访问 http://localhost:7860

**方式二：Windows 双击启动**

双击项目根目录下的 `启动Web界面.bat`，自动打开浏览器。

### 2.4 首次运行

首次启动时，PaddleOCR 会自动下载模型文件（约 500 MB），下载到：
- Windows: `C:\Users\<用户名>\.paddlex\official_models\`
- Linux: `~/.paddlex/official_models/`

下载的模型：
| 模型 | 用途 |
|------|------|
| PP-OCRv5_server_det | 文字检测 |
| PP-OCRv5_server_rec | 文字识别 |

如果网络环境受限，可提前手动下载模型放到对应目录。

### 2.5 使用流程

1. **上传文件** — 支持 PDF、TIF、TIFF、JPG、JPEG、PNG
2. **选择检测首字母** — 默认 X、Y，按需勾选
3. **点击"检测预览"** — 系统自动识别区域并标注彩色框
4. **确认无误后点击"确认替换"** — 执行替换并显示结果
5. **替换完成** — 输出文件保存在 `output/latest/vector/` 或 `output/latest/ocr/`

### 2.6 项目目录结构

```
Mitsubishi-Electric-Drawing-Replacement-Project/
├── web_app.py              # Web界面入口
├── main.py                 # CLI入口
├── config.py               # 全局配置
├── requirements_local.txt  # 本地依赖
├── 启动Web界面.bat          # Windows启动脚本
├── fonts/                  # 替换用字体
│   └── dingliesongtypeface20241217-2.ttf  # 当前使用的替换字体
├── modules/
│   ├── batch_processor.py      # 批量处理调度
│   ├── region_detector.py      # 区域检测（红/绿/橙框）
│   ├── text_replacer.py        # OCR文字替换
│   ├── pdf_vector_handler.py   # 矢量PDF替换
│   └── file_ingestion.py       # 文件加载（PDF/TIF/JPG → numpy）
├── output/                 # 替换结果输出
│   └── latest/
│       ├── vector/         # 矢量PDF替换结果
│       └── ocr/            # OCR替换结果
└── api/                    # REST API（可选）
```

### 2.7 性能参考

| 模式 | 单张图纸耗时 | 说明 |
|------|------------|------|
| 矢量PDF | 1-3 秒 | 直接文本操作，极快 |
| OCR（CPU） | 20-30 秒 | 含模型推理 |
| OCR（GPU） | 5-10 秒 | 需要 CUDA 环境 |

### 2.8 常见问题

**Q: 启动时报 `paddlepaddle` 安装错误？**
A: 确认 Python 版本为 3.11 或 3.12，不支持 3.14。

**Q: OCR 识别速度很慢？**
A: CPU模式下单张约 20-30 秒属正常。可在 Web 界面"模型设置"中切换到 GPU 加速（需安装 `paddlepaddle-gpu` + CUDA）。

**Q: 首次启动卡在模型下载？**
A: PaddleOCR 需下载约 500 MB 模型文件，请确保网络通畅或手动下载。

**Q: 替换后字体不对？**
A: 确认 `fonts/dingliesongtypeface20241217-2.ttf` 文件存在。
